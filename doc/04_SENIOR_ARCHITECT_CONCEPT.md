# Концепция улучшений: От Middle к Senior Data Architect

## Текущее состояние

Реализован MVP AI-архитектора метрик со следующими возможностями:
- ✅ Формирование Data Contracts через асинхронные переписки
- ✅ Роутинг запросов (cheap/heavy модели)
- ✅ Онбординг участников и профилирование
- ✅ Система напоминаний с эскалацией
- ✅ Версионирование контрактов
- ✅ Процедура консента для разрешения тупиков
- ✅ Симулятор для тестирования

**Текущий уровень**: Middle Data Architect — реактивный фасилитатор, который помогает согласовывать контракты по запросу.

**Целевой уровень**: Senior Data Architect — проактивный стратегический партнер, который не только формирует контракты, но и:
- Выявляет системные проблемы в архитектуре данных
- Предвидит конфликты и противоречия
- Обеспечивает целостность и качество системы метрик
- Строит семантические связи между сущностями
- Формирует культуру data governance

---

## 1. Проактивный анализ и обнаружение проблем

### 1.1 Автоматическое обнаружение конфликтов метрик

**Проблема**: Сейчас агент реагирует только на явные запросы. Многие конфликты остаются незамеченными.

**Решение**: Проактивный анализатор метрик

```python
# Новый компонент: src/analyzer.py
class MetricsAnalyzer:
    def detect_conflicts(self) -> list[Conflict]:
        """Обнаруживает конфликты между контрактами."""
        # 1. Одинаковые названия, разные формулы
        # 2. Пересекающиеся определения
        # 3. Циклические зависимости
        # 4. Разрыв в цепочке метрик от Extra Time
```

**Триггеры анализа**:
- После согласования каждого нового контракта
- Еженедельный полный аудит всех контрактов
- По команде контролёра

**Вывод**: Агент создаёт тред в канале с найденными конфликтами:
```
🔍 Проактивный анализ: обнаружен конфликт

Метрики WIN и WIN NI определяют "новую продажу" по-разному:
- WIN (Sales): подписанный контракт
- WIN NI (Finance): оплаченный инвойс

Это может привести к расхождению на 15-20% в отчётах.

Предлагаю согласовать: @sales_lead @finance_lead
```

### 1.2 Граф зависимостей метрик

**Проблема**: Неясно, как метрики связаны друг с другом и с Extra Time.

**Решение**: Построение и визуализация графа зависимостей

**Структура данных**:
```json
// contracts/graph.json
{
  "nodes": [
    {
      "id": "extra_time",
      "type": "strategic",
      "layer": 0
    },
    {
      "id": "mau",
      "type": "operational",
      "layer": 1,
      "contract": "contracts/mau.md"
    }
  ],
  "edges": [
    {
      "from": "extra_time",
      "to": "mau",
      "type": "depends_on",
      "formula": "Extra Time = f(MAU, ...)"
    }
  ]
}
```

**Новые команды**:
- "Покажи граф зависимостей для MAU"
- "Какие метрики влияют на Extra Time?"
- "Найди метрики-сироты" (не связаны с Extra Time)

**Вывод**: ASCII-граф в Mattermost
```
Extra Time
├── MAU
│   ├── New Clients
│   │   └── WIN NI ✅
│   ├── Retention
│   │   └── Churn Rate 🔄 (в работе)
│   └── Activation ⚠️ (не определена)
├── Jobs per user
│   └── ...
```

### 1.3 Обнаружение метрик-призраков

**Проблема**: Люди используют метрики, которые не определены в контрактах.

**Решение**: Анализатор упоминаний метрик в переписке

```python
class GhostMetricsDetector:
    def scan_channel_history(self) -> list[str]:
        """Сканирует историю канала на упоминания метрик."""
        # Извлекает названия метрик из сообщений
        # Сверяет с существующими контрактами
        # Возвращает список неопределённых метрик
```

**Триггер**: Еженедельно

**Вывод**:
```
📊 Обнаружены упоминания метрик без контрактов:

- "Conversion rate" — упомянута 7 раз (@sales_lead, @marketing)
- "ARPU" — упомянута 4 раза (@finance, @product_lead)
- "NPS" — упомянута 12 раз (@product_lead, @support)

Добавить в очередь на согласование?
```

---

## 2. Интеллектуальная поддержка формирования контрактов

### 2.1 Шаблоны и best practices

**Проблема**: Качество контрактов зависит от того, насколько хорошо stakeholder'ы отвечают на вопросы.

**Решение**: Библиотека паттернов контрактов

**Структура**:
```
templates/
├── patterns/
│   ├── conversion_metrics.md      # Шаблон для метрик конверсии
│   ├── financial_metrics.md       # Шаблон для финансовых метрик
│   ├── user_engagement.md         # Шаблон для вовлечённости
│   └── ...
└── examples/
    ├── win_ni_example.md          # Примеры хороших контрактов
    └── ...
```

**Логика**:
1. При создании нового контракта агент определяет тип метрики (конверсия, финансы, вовлечённость)
2. Загружает соответствующий шаблон
3. Адаптирует вопросы под специфику метрики
4. Предлагает примеры из схожих контрактов

**Пример**:
```
Вижу, что WIN NI — это метрика конверсии (продажа).

Для таких метрик важно определить:
1. Момент фиксации (подписание/оплата/активация?)
2. Временнóе окно (текущий квартал/месяц/всё время?)
3. Фильтры (типы клиентов, продуктов)

Пример схожего контракта: см. contracts/win.md

Начнём с первого вопроса: когда именно фиксируется "новая продажа"?
```

### 2.2 Автоматическая валидация контрактов

**Проблема**: Контракты могут быть неполными или противоречивыми.

**Решение**: Валидатор контрактов

```python
class ContractValidator:
    def validate(self, contract_md: str) -> ValidationReport:
        """Проверяет полноту и качество контракта."""
        checks = [
            self._check_all_sections_present(),
            self._check_formula_clarity(),
            self._check_data_source_exists(),
            self._check_responsible_assigned(),
            self._check_extra_time_linkage(),
            self._check_no_ambiguous_terms(),
        ]
        return ValidationReport(checks)
```

**Критерии качества**:
- ✅ Все секции заполнены
- ✅ Формула однозначна (нет "примерно", "около")
- ✅ Источник данных конкретный (система + поля)
- ✅ Назначен ответственный
- ✅ Прослеживается связь с Extra Time
- ✅ Нет двусмысленных терминов

**Триггер**: Перед финализацией контракта

**Вывод**:
```
⚠️ Контракт требует доработки (качество: 7/10)

Критические проблемы:
- Формула содержит неопределённость: "примерно за квартал"
- Не указан ответственный за расчёт

Рекомендации:
- Уточни: "за текущий квартал" или "за последние 90 дней"?
- Назначь ответственного из круга Analytics & Data

Хочешь сохранить как есть или доработаем?
```

### 2.3 Предложения по улучшению формул

**Проблема**: Stakeholder'ы могут предложить формулу, которая технически сложна или избыточна.

**Решение**: Упрощающий анализатор формул

**Примеры**:
```
Предложенная формула:
WIN NI = SUM(deals WHERE status='closed' AND type='new' AND quarter=current AND validated=true)

Рекомендация агента:
Эту формулу можно упростить, если ввести поле deal_category:
WIN NI = COUNT(deals WHERE deal_category='win_ni' AND quarter=current)

Это уменьшит количество условий и снизит риск ошибки.
```

---

## 3. Адаптивные стратегии переговоров

### 3.1 Профилирование стилей коммуникации

**Проблема**: Сейчас агент одинаково общается со всеми. Некоторым нужны детали, другим — краткость.

**Решение**: Расширенное профилирование участников

**Новые поля в профиле**:
```markdown
## Профиль коммуникации

### Стиль мышления
- Тип: аналитический / интуитивный / практический
- Принятие решений: данные / опыт / консенсус

### Предпочтения
- Детализация: высокая / средняя / минимальная
- Формат: варианты на выбор / открытые вопросы / примеры
- Скорость: быстрые решения / обдумывание

### Триггеры эффективности
- Хорошо реагирует на: конкретные примеры клиентов
- Плохо реагирует на: абстрактные рассуждения
- Убеждающий аргумент: влияние на выручку
```

**Сбор данных**:
- Автоматический анализ паттернов ответов
- Явные вопросы при онбординге
- Обновление профиля после каждого взаимодействия

**Применение**:
```python
# Перед формулированием вопроса
profile = self.memory.get_participant_profile(username)

if profile.style == "analytical":
    question = "Какие конкретно поля из CRM используются для расчёта?"
elif profile.style == "intuitive":
    question = "Как ты обычно объясняешь эту метрику новичкам?"
elif profile.style == "practical":
    question = "Покажи пример: какая сделка считается WIN NI, а какая нет?"
```

### 3.2 Динамическая эскалация

**Проблема**: Текущая эскалация линейна (день 2 → день 4 → день 6). Не учитывает контекст.

**Решение**: Адаптивная эскалация на основе:
- Критичности контракта
- Истории ответов участника
- Количества зависимых контрактов

**Логика**:
```python
def calculate_next_reminder(reminder: dict, participant_profile: dict) -> timedelta:
    base_interval = timedelta(days=2)

    # Участник обычно отвечает быстро → подожди дольше
    if participant_profile.avg_response_time < 6_hours:
        base_interval *= 1.5

    # Контракт критичный → напоминай чаще
    if reminder.contract.priority == "high":
        base_interval *= 0.7

    # Уже было 3+ напоминания → упрости до бинарного выбора раньше
    if reminder.escalation_step >= 3:
        base_interval *= 0.5

    return base_interval
```

### 3.3 Техники разрешения конфликтов

**Проблема**: При конфликтах агент предлагает варианты A/B. Этого недостаточно.

**Решение**: Арсенал техник переговоров

**Техники**:
1. **Расширение пирога**: Найти решение, которое удовлетворяет обоих
   ```
   @sales и @finance считают WIN по-разному.

   Вместо выбора одного варианта, предлагаю два показателя:
   - WIN Signed (момент подписания) — для Sales
   - WIN Paid (момент оплаты) — для Finance

   Оба валидны для своих целей. Согласны?
   ```

2. **Поиск общих интересов**:
   ```
   Вы спорите о включении trial-клиентов в MAU.

   Общая цель: понять, сколько людей реально используют продукт.

   Trial даёт искажение? → Два показателя:
   - MAU Total (все)
   - MAU Paid (только платящие)
   ```

3. **Временное решение**:
   ```
   Не можем согласовать определение Churn прямо сейчас?

   Предлагаю временный контракт на квартал:
   - Используем определение от @sales
   - Через квартал пересмотрим на основе реальных данных

   Согласны попробовать?
   ```

---

## 4. Семантический слой и онтология

### 4.1 Бизнес-глоссарий

**Проблема**: Разные люди используют разные термины для одного и того же.

**Решение**: Централизованный глоссарий терминов

**Структура**:
```json
// context/glossary.json
{
  "terms": [
    {
      "canonical": "Новая продажа",
      "aliases": ["new sale", "win", "сделка", "контракт"],
      "definition": "см. contracts/win_ni.md",
      "related": ["renewal", "expansion"]
    },
    {
      "canonical": "Клиент",
      "aliases": ["customer", "account", "компания"],
      "definition": "Юридическое лицо, купившее продукт",
      "disambiguation": {
        "user": "Физическое лицо, использующее продукт",
        "lead": "Потенциальный клиент"
      }
    }
  ]
}
```

**Применение**:
- При формировании контракта агент предлагает канонический термин
- При обнаружении неоднозначности — уточняет
- В ответах на вопросы — ссылается на глоссарий

**Пример**:
```
@user: "Как мы считаем клиентов?"

Агент: "Уточню: ты имеешь в виду 'клиентов' (юр.лица) или 'пользователей' (физ.лица)?

По глоссарию:
- Клиент = юридическое лицо (см. contracts/client.md)
- Пользователь = физ.лицо, использующее продукт (см. contracts/user.md)
```

### 4.2 Семантические связи

**Проблема**: Контракты живут изолированно, непонятны их связи.

**Решение**: Типизированные связи между контрактами

**Типы связей**:
```json
{
  "relationships": [
    {
      "from": "win_ni",
      "to": "win",
      "type": "subset_of",
      "description": "WIN NI — подмножество WIN (только новые продажи)"
    },
    {
      "from": "mau",
      "to": "active_user",
      "type": "aggregates",
      "description": "MAU считается как COUNT(active_user)"
    },
    {
      "from": "churn_rate",
      "to": "mau",
      "type": "inverse",
      "description": "Churn снижает MAU"
    }
  ]
}
```

**Автоматическое обнаружение связей**:
- При упоминании одной метрики в определении другой
- При анализе формул
- При обсуждениях (когда stakeholder говорит "это связано с...")

**Вывод связей**:
```
Контракт WIN NI связан с:
↑ Является подмножеством: WIN
↓ Влияет на: Revenue, NI
→ Используется в формуле: CAC
⚠️ Конфликтует с: WIN (Sales) — разные определения момента фиксации
```

### 4.3 Иерархия метрик

**Проблема**: Неясно, какие метрики стратегические, а какие операционные.

**Решение**: Классификация метрик по слоям

**Слои**:
1. **North Star** (1 метрика): Extra Time
2. **Strategic** (3-5 метрик): MAU, Jobs per user, Saved time per job
3. **Operational** (10-15 метрик): WIN NI, Churn, Activation, Retention
4. **Diagnostic** (20+ метрик): Conversion rates, feature usage, etc.

**Автоматическое определение слоя**:
```python
def classify_metric(contract: Contract) -> Layer:
    # Расстояние от Extra Time в графе зависимостей
    distance = graph.shortest_path(contract.id, "extra_time")

    if distance == 0:
        return Layer.NORTH_STAR
    elif distance == 1:
        return Layer.STRATEGIC
    elif distance == 2:
        return Layer.OPERATIONAL
    else:
        return Layer.DIAGNOSTIC
```

**Применение**:
- Приоритизация очереди контрактов (Strategic → Operational → Diagnostic)
- Разная глубина проработки (Strategic требует больше детализации)
- Разная частота пересмотра (Strategic — раз в год, Diagnostic — раз в квартал)

---

## 5. Governance и процессы

### 5.1 Lifecycle контрактов

**Проблема**: Контракты создаются, но не обновляются. Устаревают.

**Решение**: Управление жизненным циклом

**Статусы контракта**:
```
Draft → In Review → Approved → Active → Deprecated → Archived
```

**Автоматические переходы**:
- **Draft → In Review**: при создании треда согласования
- **In Review → Approved**: при достижении консента
- **Approved → Active**: через 1 неделю после Approved (grace period)
- **Active → Deprecated**: через 6 месяцев без использования
- **Deprecated → Archived**: через 3 месяца после Deprecated

**Триггеры пересмотра**:
```python
# Автоматический аудит контрактов
def should_review(contract: Contract) -> bool:
    # 1. Прошло 6+ месяцев с последнего обновления
    if contract.days_since_update > 180:
        return True

    # 2. Источник данных изменился (новая система)
    if contract.data_source.changed:
        return True

    # 3. Обнаружены конфликты с другими контрактами
    if analyzer.has_conflicts(contract):
        return True

    # 4. Частые вопросы от пользователей о контракте
    if contract.question_frequency > 5_per_month:
        return True

    return False
```

**Уведомление о пересмотре**:
```
⏰ Контракт WIN NI требует пересмотра

Причина: прошло 7 месяцев с последнего обновления

Изменения с момента последнего пересмотра:
- Источник данных изменился: CRM v1 → CRM v2
- Обнаружен конфликт с контрактом WIN (Sales)
- Получено 12 вопросов от пользователей о границах метрики

Запланировать пересмотр на эту неделю? @sales_lead @dd_lead
```

### 5.2 Политики согласования

**Проблема**: Для всех контрактов одинаковая процедура. Критичные контракты требуют больше согласований.

**Решение**: Гибкие политики согласования

**Классификация контрактов**:
```json
{
  "governance_policies": {
    "tier_1": {
      "description": "Стратегические метрики (Extra Time, MAU, Revenue)",
      "approval_required": ["ceo", "cfo", "circle_lead"],
      "consensus_threshold": 1.0,
      "review_frequency": "annually"
    },
    "tier_2": {
      "description": "Операционные метрики (WIN NI, Churn)",
      "approval_required": ["circle_lead", "data_lead"],
      "consensus_threshold": 0.8,
      "review_frequency": "quarterly"
    },
    "tier_3": {
      "description": "Диагностические метрики",
      "approval_required": ["data_lead"],
      "consensus_threshold": 0.6,
      "review_frequency": "as_needed"
    }
  }
}
```

**Применение**:
```
Контракт Extra Time (Tier 1) требует согласования:
- ✅ @alexey (CEO)
- ✅ @finance_lead (CFO)
- 🔄 @product_lead (Circle Lead) — жду ответа

Без полного консенса (3/3) контракт не может быть финализирован.
```

### 5.3 Отчётность и метрики качества

**Проблема**: Непонятно, насколько эффективно работает система Data Contracts.

**Решение**: Метрики качества системы контрактов

**Метрики**:
```markdown
## Метрики системы Data Contracts

### Покрытие
- Всего метрик в компании: 47
- Определены в контрактах: 23 (49%)
- В работе: 5 (11%)
- Не определены: 19 (40%)

### Качество
- Средняя оценка полноты контрактов: 8.2/10
- Контрактов с конфликтами: 2 (9%)
- Контрактов требующих пересмотра: 4 (17%)

### Использование
- Запросов контрактов в месяц: 127
- Топ запрашиваемые: WIN NI (34), MAU (28), Churn (19)
- Метрики-призраки (не запрашиваются): 6

### Эффективность процесса
- Среднее время согласования: 8.5 дней
- Контрактов согласовано в этом квартале: 12
- Средний уровень консенса: 0.92
```

**Дашборд в Mattermost**:
```
/contracts stats

📊 Статистика Data Contracts (Q1 2026)

Покрытие: ████████░░ 49% (23/47)
Качество: ████████░░ 8.2/10
Время согласования: 8.5 дней ⬇️ (было 12)

🏆 Топ-3 активных stakeholder'ов:
1. @sales_lead — 8 контрактов
2. @product_lead — 6 контрактов
3. @finance — 5 контрактов

⚠️ Требует внимания:
- 4 контракта требуют пересмотра
- 19 метрик без контрактов
```

---

## 6. Контекстная память и обучение

### 6.1 Институциональная память

**Проблема**: Агент не помнит историю решений и их обоснование.

**Решение**: Расширенный журнал решений с контекстом

**Формат**:
```json
// memory/decisions_enhanced.jsonl
{
  "date": "2026-03-05",
  "contract": "win_ni",
  "decision": "WIN NI фиксируется в момент подписания контракта, не оплаты",
  "rationale": "Sales использует для прогнозирования, Finance — для отчётности. Разделили на два показателя.",
  "alternatives_considered": [
    "Использовать момент оплаты (отклонён — задержка 30+ дней)",
    "Использовать момент активации (отклонён — не все клиенты активируются сразу)"
  ],
  "stakeholders": {
    "sales_lead": {
      "position": "Подписание контракта",
      "argument": "Нам нужно видеть pipeline в реальном времени"
    },
    "finance_lead": {
      "position": "Оплата инвойса",
      "argument": "Контракт может быть подписан, но не оплачен — это не выручка"
    }
  },
  "conflict_resolution": "expanded_pie",
  "related_contracts": ["win", "revenue", "pipeline"],
  "extra_time_impact": "Indirect — влияет через прогнозирование роста MAU"
}
```

**Применение**:
- При новом схожем контракте — предлагать готовое решение
- При конфликте — показывать, как решались похожие конфликты
- При вопросе "почему так решили?" — давать полный контекст

**Пример**:
```
@user: "Почему WIN NI считается в момент подписания, а не оплаты?"

Агент: "Это решение согласовано 2026-03-05 между @sales_lead и @finance_lead.

Обоснование:
- Sales нужно видеть pipeline в реальном времени → подписание
- Finance нужно видеть реальную выручку → оплата

Решение: два показателя:
- WIN NI Signed — для прогнозирования
- WIN NI Paid — для отчётности

Рассматривались альтернативы:
✗ Момент оплаты — задержка 30+ дней, искажает pipeline
✗ Момент активации — не все активируются сразу

Полный контракт: contracts/win_ni.md"
```

### 6.2 Паттерны успешных переговоров

**Проблема**: Агент не учится на успехах и неудачах.

**Решение**: Трекинг эффективности переговорных стратегий

**Сбор данных**:
```python
class NegotiationPattern:
    stakeholder_types: tuple[str, str]  # ("analytical", "intuitive")
    conflict_type: str  # "formula", "boundary", "responsibility"
    technique_used: str  # "expanded_pie", "common_ground", "timeboxed"

    outcome: str  # "consensus", "escalated", "abandoned"
    time_to_resolution: timedelta
    satisfaction_score: float  # на основе тона последних сообщений
```

**Анализ**:
```
Паттерн #1: "Expanded Pie" для конфликтов формул
- Используется: 8 раз
- Успешность: 87.5% (7/8 консенсусов)
- Среднее время: 6.2 дня
- Лучше всего работает с: analytical + practical stakeholders

Паттерн #2: "Timeboxed decision" для нерешительных участников
- Используется: 5 раз
- Успешность: 60% (3/5)
- Среднее время: 11 дней
- Работает плохо с: intuitive stakeholders (им нужно больше времени)
```

**Применение**:
```python
def choose_negotiation_technique(conflict: Conflict) -> str:
    s1_type = profiles[conflict.stakeholder_1].style
    s2_type = profiles[conflict.stakeholder_2].style

    # Ищем самый успешный паттерн для этой комбинации
    best_pattern = patterns.most_successful(
        stakeholder_types=(s1_type, s2_type),
        conflict_type=conflict.type
    )

    return best_pattern.technique
```

### 6.3 Обучение на ошибках

**Проблема**: Агент повторяет одни и те же ошибки.

**Решение**: Журнал ошибок и корректирующих действий

**Структура**:
```json
// memory/lessons_learned.jsonl
{
  "date": "2026-03-12",
  "situation": "Предложил контракт без валидации источника данных",
  "consequence": "Пришлось переделывать через неделю — источник не содержит нужных полей",
  "stakeholders_affected": ["@dd_lead", "@sales_lead"],
  "correction": "Добавлена проверка: перед финализацией контракта валидировать доступность полей в источнике",
  "rule_added": "rules/validate_data_source.py",
  "severity": "medium"
}
```

**Новые правила**:
```python
# Автоматически добавляемые проверки
class ContractValidationRules:
    rules = []

    @staticmethod
    def from_lessons_learned():
        for lesson in lessons:
            if lesson.severity in ["high", "medium"]:
                rules.append(lesson.rule)
```

---

## 7. Инструменты для контролёра

### 7.1 Дашборд управления

**Команды контролёра**:
```
/contracts dashboard              # Общая статистика
/contracts queue prioritize       # Пересортировать очередь по важности
/contracts audit [contract_id]    # Полный аудит контракта
/contracts conflicts              # Все активные конфликты
/contracts orphans                # Метрики не связанные с Extra Time
/contracts review [contract_id]   # Запланировать пересмотр
/contracts archive [contract_id]  # Архивировать контракт
```

### 7.2 Настройка правил

**Файл**: `config/governance.yaml`

```yaml
# Политики согласования
approval_policies:
  tier_1:
    metrics: ["extra_time", "mau", "revenue"]
    required_approvers: ["ceo", "cfo"]
    consensus_threshold: 1.0

# Настройки эскалации
escalation:
  base_interval_days: 2
  max_step: 5
  escalation_user: "@alexey"

# Автоматические проверки
automated_checks:
  conflict_detection: weekly
  ghost_metrics_scan: weekly
  contract_review_audit: monthly

# Качество контрактов
quality_thresholds:
  min_score: 7.0
  critical_sections: ["formula", "data_source", "responsible"]
```

### 7.3 Ручное вмешательство

**Команды**:
```
/override [contract_id] approve    # Принудительное согласование
/override [contract_id] reject     # Отклонить контракт
/escalate [contract_id] to @user   # Эскалировать конкретному человеку
/pause [contract_id]               # Приостановить согласование
/resume [contract_id]              # Возобновить
```

---

## Приоритизация реализации

### Фаза 1: Проактивность (2 недели)
1. Автоматическое обнаружение конфликтов метрик
2. Граф зависимостей метрик
3. Обнаружение метрик-призраков
4. Валидация контрактов перед финализацией

**Критерий успеха**: Агент находит 3+ конфликта в существующих контрактах без явного запроса.

### Фаза 2: Интеллект (2 недели)
1. Шаблоны контрактов по типам метрик
2. Семантические связи между контрактами
3. Бизнес-глоссарий
4. Расширенное профилирование участников

**Критерий успеха**: Качество новых контрактов увеличивается с 7.0 до 8.5/10.

### Фаза 3: Governance (1 неделя)
1. Lifecycle контрактов (статусы, триггеры пересмотра)
2. Политики согласования по уровням
3. Метрики качества системы
4. Дашборд для контролёра

**Критерий успеха**: Контролёр видит полную картину системы контрактов в одной команде.

### Фаза 4: Память и обучение (1 неделя)
1. Расширенный журнал решений с обоснованиями
2. Паттерны успешных переговоров
3. Журнал ошибок и корректирующих действий
4. Адаптивные стратегии переговоров

**Критерий успеха**: Агент предлагает готовое решение для 70%+ типовых конфликтов.

---

## Измеримые результаты

### Текущее состояние (Middle)
- Время согласования контракта: **12 дней**
- Качество контрактов: **7.0/10**
- Покрытие метрик контрактами: **40%**
- Обнаружение конфликтов: **реактивно** (только по запросу)
- Пересмотр контрактов: **никогда**

### Целевое состояние (Senior)
- Время согласования контракта: **6 дней** (-50%)
- Качество контрактов: **8.5/10** (+1.5)
- Покрытие метрик контрактами: **85%** (+45 п.п.)
- Обнаружение конфликтов: **проактивно** (автоматически еженедельно)
- Пересмотр контрактов: **ежеквартально** для критичных метрик

### Качественные улучшения
- ✅ Агент предвидит конфликты до их возникновения
- ✅ Контракты содержат полное обоснование решений
- ✅ Все метрики прослеживаются до Extra Time
- ✅ Stakeholder'ы получают персонализированную коммуникацию
- ✅ Система контрактов самоочищается от устаревших определений
- ✅ Контролёр видит полную картину качества данных

---

## Технологический стек

### Новые зависимости
```txt
# Анализ графов
networkx>=3.0

# Валидация YAML
pyyaml>=6.0

# NLP для анализа текста
spacy>=3.7
```

### Новые компоненты
```
src/
├── analyzer.py              # Анализатор конфликтов и зависимостей
├── validator.py             # Валидатор контрактов
├── negotiator.py            # Стратегии переговоров
├── graph_builder.py         # Построение графа зависимостей
├── pattern_matcher.py       # Поиск паттернов и шаблонов
└── governance.py            # Управление lifecycle и политиками

templates/
├── patterns/                # Шаблоны контрактов по типам
└── examples/                # Примеры хороших контрактов

config/
└── governance.yaml          # Политики и правила

memory/
├── decisions_enhanced.jsonl # Расширенный журнал решений
├── lessons_learned.jsonl    # Журнал ошибок
└── negotiation_patterns.json # Паттерны переговоров

contracts/
├── graph.json               # Граф зависимостей
└── glossary.json            # Бизнес-глоссарий
```

---

## Вывод

Переход от **Middle** к **Senior Data Architect** — это переход от **реактивного фасилитатора** к **проактивному стратегическому партнёру**.

Senior архитектор:
- **Видит системно**: понимает граф зависимостей, обнаруживает конфликты
- **Думает наперёд**: предвидит проблемы, предлагает решения
- **Обеспечивает качество**: валидирует контракты, поддерживает lifecycle
- **Адаптируется**: учитывает стили коммуникации, учится на опыте
- **Управляет знаниями**: строит глоссарий, формирует институциональную память

При этом **остаётся в рамках контрактов и коммуникации** — без подключения к PostgreSQL.

Следующий шаг после реализации этих улучшений — интеграция с базой данных для автоматической валидации данных и мониторинга соблюдения контрактов.
