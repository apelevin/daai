# ── LLM (OpenRouter) ─────────────────────────────────────────
# Required. API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxx

# Optional. Override base URL (default: https://openrouter.ai/api/v1)
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional. Override models (defaults shown)
# CHEAP_MODEL=anthropic/claude-3-5-haiku-20241022
# HEAVY_MODEL=anthropic/claude-sonnet-4-5-20250929

# Optional. Fallback model for empty-reply recovery (default: google/gemini-3-flash-preview)
# FALLBACK_MODEL=google/gemini-3-flash-preview

# Optional. Log token usage per call (default: true)
# LOG_LLM_COSTS=true

# ── Mattermost ───────────────────────────────────────────────
# Required. Mattermost server URL
MATTERMOST_URL=https://mattermost.example.com

# Required. Channel ID for Data Contracts channel
DATA_CONTRACTS_CHANNEL_ID=

# Auth: either bot token OR login+password
MATTERMOST_BOT_TOKEN=
# MATTERMOST_LOGIN=
# MATTERMOST_PASSWORD=

# Optional. Team ID (for channel creation script)
# MATTERMOST_TEAM_ID=

# ── Data ─────────────────────────────────────────────────────
# Optional. Base directory for all data files (default: current dir)
# DATA_DIR=.

# ── Scheduler ────────────────────────────────────────────────
# Optional. Username to escalate to (default: alexey)
# ESCALATION_USER=alexey

# Optional. Hours between reminder checks (default: 4)
# REMINDER_CHECK_HOURS=4

# ── Open Questions Digest ─────────────────────────────────────
# Optional. Time to post daily open questions digest (default: 10:00)
# OPEN_QUESTIONS_DIGEST_TIME=10:00

# Optional. Workdays as comma-separated weekday numbers 0=Mon..4=Fri (default: 0,1,2,3,4)
# OPEN_QUESTIONS_DIGEST_WORKDAYS=0,1,2,3,4

# Optional. Mattermost team slug for building post URLs (required for thread links in digest)
# MATTERMOST_TEAM_NAME=myteam

# ── Continuous Planner ────────────────────────────────────────
# Optional. Time to run daily planning cycle (default: 09:00, use "now" to force)
# PLANNER_RUN_TIME=09:00

# Optional. Workdays as comma-separated weekday numbers 0=Mon..4=Fri (default: 0,1,2,3,4)
# PLANNER_WORKDAYS=0,1,2,3,4

# Optional. Max concurrent active initiatives (default: 3)
# PLANNER_MAX_ACTIVE_INITIATIVES=3

# Optional. Max new threads per day (default: 2)
# PLANNER_MAX_NEW_THREADS_PER_DAY=2

# Optional. Max messages per day from planner (default: 8)
# PLANNER_MAX_MESSAGES_PER_DAY=8

# Optional. Max actions per initiative per day (default: 2)
# PLANNER_MAX_ACTIONS_PER_INITIATIVE_PER_DAY=2

# Optional. Cooldown hours before re-raising same topic (default: 48)
# PLANNER_COOLDOWN_HOURS=48

# Optional. Hours to wait before follow-up (default: 24)
# PLANNER_WAIT_BEFORE_FOLLOWUP_HOURS=24

# Optional. Days before abandoning stale initiative (default: 14)
# PLANNER_STALE_INITIATIVE_DAYS=14

# ── Dashboard ───────────────────────────────────────────────
# Optional. Dashboard web UI host and port (default: 0.0.0.0:8050)
# DASHBOARD_HOST=0.0.0.0
# DASHBOARD_PORT=8050

# ── Logging ──────────────────────────────────────────────────
# Optional. Log level (default: INFO)
# LOG_LEVEL=INFO
